# Configuration for the installed-wheel tests.  They must import the
# installed elliprof package, never the source tree, so this file
# replaces the project configuration (no pythonpath entries).
[pytest]
addopts = -p no:cacheprovider
